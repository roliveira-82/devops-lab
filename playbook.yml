- hosts: terraform_ansible
  tasks:
    - name: Cria arquivo index.html
      copy:
        dest: /home/ubuntu/index.html
        content: "<h1>Feito com Ansible</h1>"

    - name: Sobe servidor HTTP
      shell: nohup busybox httpd -f -p 8080 &
